import { Component, OnInit } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Customer } from '../../model/class/interface/Customer';
import { CommonModule } from '@angular/common';
import { HttpClient } from '@angular/common/http';

/**
 * The manager.component.ts file defines the ManagerComponent,
 *  which is part of the Angular application. This component is likely responsible
 *  for managing customer or staff-related data and operations. It includes methods
 *  and properties to interact with the backend API using Angular's HttpClient and
 *  provides a user interface for manager-specific tasks.
 */
@Component({
  selector: 'app-manager',
  imports: [FormsModule,CommonModule],
  templateUrl: './manager.component.html',
  styleUrl: './manager.component.css'
})
export class ManagerComponent {

 




  // Add your methods and properties here

}
